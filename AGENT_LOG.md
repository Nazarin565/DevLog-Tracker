# AGENT_LOG — DevLog

Honest log of work done with the coding agent (Claude Code / claude-sonnet-4-6).
Kept as we go, not written at the end.

---

## Step 9 — packages/web (Next.js UI scaffold)

**Date:** 2026-05-21

### What the agent scaffolded

All frontend code was generated by Claude Code (claude-sonnet-4-6) in an interactive session:

- `app/layout.tsx` — root layout with Tailwind, header, `Providers` wrapper
- `app/providers.tsx` — `QueryClientProvider` in a `'use client'` wrapper (required because layout is a server component)
- `app/page.tsx` — task list with status filter, sort-by, order; filter state stored in URL via `useSearchParams` / `router.push`
- `app/tasks/new/page.tsx` — create task form page
- `app/tasks/[id]/page.tsx` — task detail page with inline edit, subtask list, and AgentPanel
- `components/TaskCard.tsx` — card with priority/status badges, expandable subtask list, edit/delete buttons
- `components/TaskForm.tsx` — controlled form for create/edit with client-side required validation
- `components/SubtaskList.tsx` — renders subtasks with done/pending indicators
- `components/AgentPanel.tsx` — agent selector (decompose / prioritise), run button, reasoning trace display, subtask creation confirmation
- `hooks/useTasks.ts` — all TanStack Query hooks (`useTasks`, `useTask`, `useCreateTask`, `useUpdateTask`, `useDeleteTask`, `useCreateSubtasks`, `useRunAgent`)
- `lib/api.ts` — typed `fetch` wrapper with base URL from `NEXT_PUBLIC_API_URL`, error handling
- `app/globals.css` — Tailwind v4 import
- `postcss.config.mjs` — Tailwind v4 PostCSS plugin config

### Manual interventions and why

1. **`AgentResult` type defined locally in `lib/api.ts`** — the type lives in `packages/server/src/agents/types.ts` and is not exported from `@devlog/shared`. Rather than leaking a server-internal type into shared, defined a local interface `AgentResult<T>` with `output` and `steps` in the web package. This keeps the server/web boundary clean.

2. **`providers.tsx` created after initial `layout.tsx`** — the agent initially referenced `./providers` in `layout.tsx` before creating the file. IDE flagged `TS2307`. The providers file was created immediately after; this is a normal file-ordering artefact of sequential generation.

3. **`packages/server/.env` and `packages/web/.env.local` created for local dev** — these are gitignored. Without them the server defaults are correct (`LLM_PROVIDER=mock`) but the port/CORS config needed explicit values to start cleanly. These files are not committed; `.env.example` at root documents all variables.

4. **`useRunAgent` signature refactored — `agentId` moved to call-site** — the agent-generated version accepted `agentId` as a constructor argument (`useRunAgent<T>(agentId)`), producing a hook instance tied to one agent. This meant `AgentPanel` needed a separate hook instance per agent, which is awkward with conditional rendering. Fixed manually: `useRunAgent<T>()` takes no argument; `agentId` is passed together with `input` at `mutateAsync({ agentId, input })` time. Result: one hook instance, any agent can be called from it.

5. **Null-guards added to `AgentOutput` in `AgentPanel`** — the agent-generated code cast `output` to the expected type without validation. If the API ever returns an unexpected shape, the component would crash with a runtime type error. Added explicit guards (`if (!result || typeof result.type !== 'string')` and `if (!pResult || !Array.isArray(pResult.rankedTasks))`) that render a fallback error message instead of throwing.

6. **`AgentOutput` null-guards replaced with Zod `safeParse`** — the manual guards from fix 5 were replaced with `DecompositionResultSchema.safeParse(output)` and `PrioritisationOutputSchema.safeParse(output)`. This is strictly better: the same schemas used on the server now validate the output on the client, consistent with the project invariant "never trust LLM output, always safeParse". Manual `typeof` checks would silently pass structurally wrong data that Zod would catch.

7. **`taskTitleMap` moved from internal `useTasks()` call to prop** — the agent-generated `AgentPanel` fetched all tasks itself via `useTasks()` to resolve task IDs to titles. Fixed: `taskTitleMap` is now passed as a prop from `[id]/page.tsx`, which already has `useTasks()` in scope. This avoids a redundant duplicate network request inside the component and keeps data fetching at the page level.

### Result

- `tsc --noEmit` passes with 0 errors on `packages/web`
- `npm run dev` starts both Express (:4000) and Next.js (:3000) cleanly
- API smoke tests: CRUD, filters/sort, agent run all return correct responses
- Reasoning trace renders correctly in `AgentPanel`; subtask creation flow wired through `useCreateSubtasks` → `invalidateQueries`

---

## Step 10 (post-scaffold) — Code Review Fixes

**Date:** 2026-05-21

### Context

After scaffolding and integration (steps 9–10), a structured senior fullstack review was performed across all layers: shared contracts, db, routes, agent framework, llm layer, and web. The review used a checklist covering API design, validation, DB edge cases, type correctness, and frontend state/rendering. Six issues were found and fixed in this session. All fixes were made manually — the agent (Claude Code) identified the issues; changes were human-confirmed before writing.

### Fixes applied

1. **`order` param ignored for `sortBy=priority`** — `taskRepository.ts:72`. The `dir` variable was computed from `query.order` but hardcoded to `DESC` in the priority branch. Fixed: `ORDER BY ${PRIORITY_ORDER} ${dir}, created_at DESC`. Users selecting `sortBy=priority&order=asc` now get low→medium→high.

2. **Agent route passed `req.body` to agents without validation** — `routes/agents.ts:22`. Raw `req.body` was forwarded directly to agents, so a malformed body (e.g. missing `taskId`) would cause an opaque runtime error inside the agent instead of a clean 400. Fixed: added `DecomposeInputSchema` and `PrioritiseInputSchema` to `packages/shared/src/agents.ts`; `routes/agents.ts` now looks up the schema by `agentId` and calls `.parse(req.body)` before passing input to the agent. ZodError → 400 is handled by the existing `errorHandler` middleware. This enforces the project invariant "validate at system boundaries."

3. **`api.subtasks.create` typed as `Promise<Task>` instead of `Promise<Subtask[]>`** — `web/lib/api.ts:58`. The server route `POST /api/tasks/:id/subtasks` returns `Subtask[]` (201), but the client wrapper was typed as `Promise<Task>`. TypeScript did not catch this because `request<T>` uses `as Promise<T>`. Fixed: changed to `Promise<Subtask[]>` in `api.ts` and added explicit generic `useMutation<Subtask[], Error, CreateSubtasksInput>` in `useTasks.ts`.

4. **`SubtaskProposal.description` silently dropped on subtask creation** — `shared/src/agents.ts:17`. `SubtaskProposalSchema` had an optional `description` field, but `CreateSubtasksSchema` and the DB schema only accept `title`. Any `description` returned by the LLM was silently discarded. Fixed: removed `description` from `SubtaskProposalSchema` — the LLM prompt does not ask for it, the DB does not store it, so the field was phantom. Keeping a field that is always dropped is misleading; the cleaner fix is to remove it rather than expand DB schema for unused data.

5. **Empty `IN()` edge case in `taskRepository.findAll`** — `taskRepository.ts:80`. When `findAll` returned zero task rows, the subtask query generated `WHERE task_id IN (NULL)` via a `|| 'NULL'` fallback. This worked in SQLite but was an opaque workaround. Fixed: added an early `if (rows.length === 0) return []` guard before the subtask query, and removed the `|| 'NULL'` fallback. The IN-clause now only runs when there are actual task IDs.

6. **`key={i}` (array index) in AgentPanel lists** — `AgentPanel.tsx:100, 140, 153`. Three lists used index as React key: reasoning steps, clarify questions, proposed subtasks. Fixed: changed to composite keys (`${step.label}-${i}`, `q-${i}`, `st-${i}`) which are stable within a single agent result.

### What was NOT fixed (intentional scope decisions)

- **No routes for `subtaskRepo.setDone`/`remove`**: `subtaskRepository.ts` has `setDone` and `remove` methods but no corresponding HTTP routes. `SubtaskList` displays done/pending state but cannot toggle it from the UI. Kept as a conscious scope decision — adding toggle would require a new route (`PATCH /api/tasks/:id/subtasks/:subId`) and UI affordance. Flagged as a future improvement, not a bug.
- **Non-deterministic subtask ordering**: `createMany` sets a single `now` timestamp for all subtasks in a batch, so `ORDER BY created_at ASC` between them is non-deterministic. Left as-is — the practical impact is negligible and fixing it would require either a sequence column or inserting timestamps with millisecond offsets.
- **`confirm()` for delete**: two pages use native `window.confirm`. Functional, but not styled and can be suppressed in some environments. Left as-is for scope.

### Result

- `npm run typecheck` → exit 0 (all packages)
- `npm test` → 14/14 tests pass
